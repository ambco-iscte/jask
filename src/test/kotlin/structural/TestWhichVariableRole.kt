package structural

import org.junit.jupiter.api.Test

class TestWhichVariableRole {

    @Test
    fun `supposed to fail for now (i'm lazy)`() {
        TODO("Supposed to fail for now")
    }

    @Test
    fun `also supposed to fail for now (i'm still lazy)`() {
        TODO("Supposed to fail for now")
    }
}